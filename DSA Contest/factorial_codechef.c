#include<stdio.h>

int main(void)
{
    size_t number;

    scanf("%zu",&number);
    size_t zeroes[number];

    // for(size_t i=0;i<number;i++)
    // {
    //     scanf("%zu",&arr[i]);
    // }

    while(number--)
    {   
        int var;
        scanf("%d",&var);
    

        size_t counter=0; // it counts no of 5 in a programe

        
      
    }

    for(int i=0;i<number;i++)
    {
        printf("%d",zeroes[i]);
    }



   
    return 0;
}